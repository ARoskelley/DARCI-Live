# TestProject

A small test project for DARCI coding loop validation.

## Overview

Contains an order processing system with payment gateway integration.

- `OrderProcessor` — validates and processes orders
- `PaymentGateway` — charges payment methods
- `Models` — shared data types
