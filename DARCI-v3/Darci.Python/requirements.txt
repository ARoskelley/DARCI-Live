fastapi
uvicorn

# CAD generation pipeline
cadquery            # parametric CAD — install via conda: conda install -c cadquery -c conda-forge cadquery
trimesh             # mesh validation, bounding box, watertight checks
numpy               # required by trimesh and rendering
Pillow              # image handling for rendered views
pyrender            # orthographic view rendering (optional, needs OpenGL)
pyglet<2            # pyrender dependency pinned for compatibility
